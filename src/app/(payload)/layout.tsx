import React from 'react'
import '@payloadcms/next/css'

export const metadata = { title: 'Ticker Tales Admin' }

export default async function PayloadLayout({ children }: { children: React.ReactNode }) {
  return children
}
